<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.sist.user.dao.userexlist">
	
	<select id="selectAllExList" resultType="ue" parameterType="int">
		select * 
		from(
			select row_number() over (ORDER BY watch_count) num,ex_num, ex_name, exhibition_poster, 
			ex_intro,ex_info,add_img,ex_hall_num,ex_status
			from exhibition
			where ex_name like ${ex_name} and ex_status like 't')
	</select>
	
	<select id="selectLocalExList" resultType="ue" >
			select * 
			from(	
				select row_number() over (ORDER BY watch_count) num,ex_num, ex_name, exhibition_poster,
						ex_intro,ex_info,add_img,ex_hall_num,ex_status
				from exhibition
			where ex_hall_num like ? and ex_status like 't')
	</select>


</mapper>